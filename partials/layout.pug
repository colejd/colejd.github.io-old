//-
    This is the base file that makes up every page on the website.
    To use, extend it and append/prepend to any of the provided blocks.

doctype html
html
    //
        Website by Jonathan Cole
        This page was auto-generated with Pug. See https://github.com/seieibob/seieibob.github.io/tree/dev

    //- Insertion point for dinosaur ASCII
    block dinosaur
    
    head
        //- Import sitewide head info
        include head.pug

        //- Hook for individual page meta info
        block meta

        // Set background color of CSS to prevent white flashing before everything has loaded
        // (if your background isn't white to start with)
        style(type='text/css').
            body {
                background-color: #fff;
            }

        // Load web fonts
        script(src="https://ajax.googleapis.com/ajax/libs/webfont/1.6.16/webfont.js")
        script.
            WebFont.load({
            google: {
                families: ['Lato:300,400,500', 'Work+Sans:500,700']
            }
            });

        // Load JQuery first
        script(src='https://code.jquery.com/jquery-3.2.1.min.js', 
            integrity='sha256-hwg4gsxgFZhOsEEamdOYGBf13FyQuiTwlAQgxVSNgt4=', 
            crossorigin='anonymous')
        
        // Load vendor CSS
        link(href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css", rel="stylesheet")
        link(href="/dist/bootstrap/css/bootstrap-accessibility.css", rel="stylesheet")
        link(href="https://netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.css", rel="stylesheet")

        // Bootstrap Core
        script(src='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js')
        script(src='/dist/bootstrap/js/bootstrap-accessibility.min.js')

        // HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries
        <!--[if lt IE 9]>
            <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
            <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
        <![endif]-->

        // Custom styles for this template
        link(href="/css/style.css", rel="stylesheet")

        // Custom head hook
        block head

    body 
        block body

        //- Include sidebar
        include sidebar.pug

        //- All page content should go in here
        div#body-container
            div.title-header
                block title
                block title-header-hook

            main.container.main-content(role='main')
                article
                    block content

        script.
            // Scroll to url anchor
            $(document).ready(function(){
                if($(location.hash).length && location.hash != "#"){ // If the tag actually exists on the page
                    $('html, body, section').animate({
                        'scrollTop': $(location.hash).offset().top
                    }, 500);
                }
            });
            
            //Scroll to clicked anchor links
            var $root = $('html, body');
            $('a').click(function(){
                var href = $.attr(this, 'href');
                if(href == null) return;
                if(href[0] != "#") return;
                if(href.length <= 1) return;
                $root.animate({
                    scrollTop: $(href).offset().top
                }, 500, function (){
                    window.location.hash = href;
                });
                return false;
            });


    foot
        block footer

        


